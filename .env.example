# ===========================================
# PDF Converter API - Environment Configuration
# ===========================================

# -----------------
# Application Settings
# -----------------
APP_NAME=PDF Converter API
APP_VERSION=1.0.0
DEBUG=False

# -----------------
# File Upload Configuration
# -----------------
MAX_FILE_SIZE_MB=50
ALLOWED_EXTENSIONS=pdf,docx,doc,xlsx,xls,jpg,jpeg,png

# -----------------
# Storage Configuration
# -----------------
UPLOAD_DIR=storage/uploads
OUTPUT_DIR=storage/outputs

# -----------------
# AWS S3 Storage Configuration (for EC2 deployment)
# -----------------
# Enable S3 storage (set to True when using AWS EC2)
USE_S3_STORAGE=False

# AWS Region where your S3 bucket is located
# Common regions: us-east-1, us-east-2, us-west-1, us-west-2, eu-west-1
AWS_REGION=us-east-2

# S3 Bucket name (create this in AWS S3 first)
# Example: pdf-converter-bucket-786
S3_BUCKET_NAME=

# Presigned URL expiration time in seconds (default: 1 hour)
S3_PRESIGNED_URL_EXPIRATION=3600

# NOTE: AWS credentials are NOT needed when using EC2 with IAM role
# The EC2 instance will automatically use the attached IAM role for S3 access

# -----------------
# Supabase Storage Configuration (alternative to S3)
# -----------------
# Enable Supabase storage (set to True when using Supabase)
USE_SUPABASE_STORAGE=False
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-service-role-key-here
SUPABASE_BUCKET_NAME=file-conversions

# -----------------
# LibreOffice Configuration
# -----------------
# Path to LibreOffice executable
# Windows: C:\Program Files\LibreOffice\program\soffice.exe
# Linux/EC2: /usr/bin/libreoffice
LIBREOFFICE_PATH=C:\Program Files\LibreOffice\program\soffice.exe

# -----------------
# File Retention
# -----------------
# Hours to keep files before cleanup (24 hours = 1 day)
FILE_RETENTION_HOURS=24

# -----------------
# Redis & Celery Configuration
# -----------------
# Redis URL (local for development, or use ElastiCache for production)
REDIS_URL=redis://localhost:6379/0

# Celery broker URL (defaults to REDIS_URL if not set)
CELERY_BROKER_URL=

# Celery result backend (defaults to REDIS_URL if not set)
CELERY_RESULT_BACKEND=

# -----------------
# CORS Configuration
# -----------------
# Update with your frontend URL(s)
# Multiple origins separated by comma
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ===========================================
# DEPLOYMENT-SPECIFIC CONFIGURATION NOTES
# ===========================================

# For LOCAL DEVELOPMENT (Windows/Mac):
# - Set USE_S3_STORAGE=False
# - Set USE_SUPABASE_STORAGE=False
# - Files stored in local storage/ directory
# - LIBREOFFICE_PATH to your local installation

# For AWS EC2 DEPLOYMENT:
# - Set USE_S3_STORAGE=True
# - Set USE_SUPABASE_STORAGE=False
# - Create S3 bucket and set S3_BUCKET_NAME
# - Attach IAM role to EC2 instance with S3 permissions
# - NO AWS credentials needed in .env file
# - LIBREOFFICE_PATH=/usr/bin/libreoffice
# - Set DEBUG=False for production
# - See AWS_EC2_DEPLOYMENT_GUIDE.md for full setup

# For SUPABASE DEPLOYMENT:
# - Set USE_SUPABASE_STORAGE=True
# - Set USE_S3_STORAGE=False
# - Provide SUPABASE_URL and SUPABASE_KEY
# - Create bucket in Supabase Storage dashboard
# - See SUPABASE_SETUP.md for configuration

# Security Notes:
# - Never commit .env file to version control
# - Add .env to .gitignore
# - Keep this file secure (chmod 600 .env on Linux)
# - Use strong passwords/keys in production
# - Rotate credentials regularly

